import { ISBPlayers, ISBPairings } from './types';
export declare function pairPlayers(players: ISBPlayers): ISBPairings;
export declare function quality(importance: number, closeness: number): number;
